<div id="microblog">
  <table cellpadding="0" cellspacing="0">
    <tbody>
    <% timeline&.each do |timestamp, entry| %>
        <tr>
          <td>&nbsp;</td>
          <td class="date">
            <%= base.format(timestamp) %>
          </td>
          <td class="entry">
            <% if entry.is_a?(Twitter::Tweet)%>
                <%= twitter.parse(entry) %>
            <% else %>
                <%= entry.image_link %>
                <%= instagram.parse(entry.text) %>
            <% end %>
          </td>
          <td>&nbsp;</td>
        </tr>
    <% end %>
      <tr>
        <td>&nbsp;</td>
        <td class="date">
          <%= base.format(Time.now) %>
        </td>
        <td class="entry">Momentan sind leider keine Neuigkeiten verfügbar. Es lohnt hingegen ein Blick auf
          <a href="https://twitter.com/spaceneedle2019" title="Twitter" target="_blank">Twitter</a> oder
          <a href="https://instagram.com/spaceneedle2019" title="Instagram" target="_blank">Instagram</a>.
        </td>
        <td>&nbsp;</td>
      </tr>
    </tbody>
  </table>
</div>
